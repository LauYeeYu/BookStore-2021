#include <iostream>
#include <fstream>

#include "unrolled_linked_list.h"

int main()
{
    while (true) {
        try {
            exit(0);
        } catch (int i) {
            std::cout << "Invalid" << std::endl;
        }
    }
    return 0;
}
